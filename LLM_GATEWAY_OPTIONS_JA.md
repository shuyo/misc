# OpenAI API 互換・複数 LLM 統合ゲートウェイ（最小依存）提案

## 前提（制約）
- **LiteLLM は採用しない**（サプライチェーン攻撃リスクおよび依存ライブラリ数の観点で要件不適合）
- 要件は以下を同時に満たすこと
  - クライアントからは **OpenAI API 互換** (`/v1/chat/completions`, `/v1/models`) に見せる
  - 背後で複数プロバイダ（OpenAI / Anthropic / Gemini / Azure OpenAI / OSS 推論サーバ）へ振り分ける
  - **ストリーミング（SSE）を安定動作** させる
  - **依存パッケージ最小化** でサプライチェーン攻撃面を縮小する

## 結論（推奨）
この条件では、**Go 標準ライブラリ中心の薄い自作プロキシ**を第一候補にするのが最適。

- 依存は原則 `net/http`, `httputil`, `context`, `crypto/tls`, `encoding/json` のみ
- 追加パッケージは「必須性を証明できるもの」以外は入れない
- まず `/v1/chat/completions` と `/v1/models` だけ実装し、機能拡張を意図的に遅らせる

## 候補比較（LiteLLM を除外）

### A. 自作（Go 標準ライブラリ中心）
- 長所
  - 依存を最小化しやすい（極端には標準ライブラリのみ）
  - 監査対象が小さく、脆弱性対応の追従が速い
  - 振る舞い（SSE, リトライ, タイムアウト）を完全に制御できる
- 短所
  - 実装・運用責任を自前で持つ必要がある
  - プロバイダ差分吸収の設計が必要

### B. one-api 系（OpenAI 互換集約）
- 長所
  - 導入が速い
- 短所
  - 依存最小化という要件には合いにくい
  - プロジェクトごとに保守品質差があるため、厳格な運用前評価が必須

## 最小依存での実装方針（推奨設計）

### 1) API 境界を固定
- 公開 API を最小化
  - `POST /v1/chat/completions`
  - `GET /v1/models`
- 互換範囲を仕様書で明示（未対応パラメータは 400 で明示的に返す）

### 2) ルーティングを単純化
- `model` 名の prefix/suffix で上流を選択（例: `anthropic/claude-*`, `openai/gpt-*`）
- ルールは静的設定ファイルで管理し、動的プラグインを避ける

### 3) SSE 安定化の必須ポイント
- upstream のストリームを可能な限り pass-through
- `http.Flusher` で逐次 `Flush()`
- 読み取り/書き込み/アイドルのタイムアウトを分離
- クライアント切断時は `context cancel` で upstream を即停止
- ストリーム中の自動リトライは禁止（重複応答リスク）

### 4) 障害時ポリシー
- 非ストリームのみ条件付きリトライ（5xx + idempotent 条件）
- ストリームは fail-fast（明確なエラー整形だけ行う）
- サーキットブレーカは最初は導入せず、必要性が出てから最小実装

### 5) サプライチェーン対策
- 依存追加は原則禁止、例外は設計審査で承認
- lockfile とチェックサム固定
- CI で SCA / ライセンス監査 / SBoM 生成を必須化
- egress 制限で接続先を上流 API のみに限定
- 実行基盤は distroless または scratch を優先

## すぐ使える意思決定
- **要件準拠を最優先**: Go 自作（標準ライブラリ中心）
- **短期 PoC 優先**でも、依存最小要件を満たさない集約製品は採用しない
- **運用方針**: 小さい機能面で開始し、監査可能性を維持したまま段階拡張
